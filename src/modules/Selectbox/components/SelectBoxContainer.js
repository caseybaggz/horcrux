// @flow

import styled from "styled-components";

const SelectBoxContainer /* : object */ = styled.div`
  box-sizing: border-box;
  display: inline-block;

  &:focus {
    outline: none;
  }
`;

export default SelectBoxContainer;
